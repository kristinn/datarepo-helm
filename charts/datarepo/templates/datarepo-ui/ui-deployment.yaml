apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "datarepo-ui.fullname" . }}
  labels:
    {{- include "datarepo-ui.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.ui.replicaCount }}
  selector:
    matchLabels:
      component: {{ include "datarepo-ui.fullname" . }}
  template:
    metadata:
      labels:
{{- include "datarepo-ui.labels" . | nindent 8 }}
        component: {{ include "datarepo-ui.fullname" . }}
##  dynamically populates environment annotations
      annotations:
{{ toYaml .Values.ui.podAnnotations | indent 8 }}
    spec:
      serviceAccountName: {{ include "datarepo-ui.serviceAccountName" . }}
      securityContext:
        runAsUser: 101
        runAsGroup: 101
        fsGroup: 101
        runAsNonRoot: true
      containers:
      - name: {{ .Chart.Name }}
        image: {{ if .Values.ui.imageName }}{{ .Values.ui.imageName }}{{ else }}"{{ .Values.ui.image.repository }}:{{ .Values.ui.image.tag }}"{{ end }}
        ports:
        - containerPort: {{ .Values.ui.nginxport }}
##  dynamically populates environment variables
        env:
        {{- if .Values.ui.env }}
        {{- range $key, $value := .Values.ui.env }}
        - name: "{{ $key }}"
          value: "{{ $value }}"
        {{- end }}
        {{- end }}
        readinessProbe:
          httpGet:
            path: /status
            port: {{ .Values.ui.nginxport }}
          initialDelaySeconds: 35
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        livenessProbe:
          httpGet:
            path: /status
            port: {{ .Values.ui.nginxport }}
          initialDelaySeconds: 45
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
          - name: nginxconf
            mountPath: /etc/nginx/
      volumes:
        - name: nginxconf
          configMap:
            name: {{ include "datarepo-ui.fullname" . }}
            items:
              - key: nginxconf
                path: nginx.conf
{{- if .Values.ui.resources }}
        resources:
{{- toYaml .Values.ui.resources | nindent 10 }}
    {{- end }}
    {{- if .Values.ui.nodeSelector }}
      nodeSelector:
{{- toYaml .Values.ui.nodeSelector | nindent 8 }}
    {{- end }}
    {{- if .Values.ui.tolerations }}
      tolerations:
{{- toYaml .Values.ui.tolerations | nindent 8 }}
    {{- end }}
    {{- if .Values.ui.affinity }}
      affinity:
{{- toYaml .Values.ui.affinity | nindent 8 }}
    {{- end }}
